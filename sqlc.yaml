version: "2"
sql:
  - schema: "sql/schema"
    queries: "sql/queries"
    engine: "postgresql"
    gen:
      go:
        out: "internal/database"
        emit_pointers_for_null_types: true
        overrides:
          - db_type: "integer"
            go_type: "int"
